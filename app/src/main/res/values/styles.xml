<resources>
    <!-- Base application theme. -->
    <style name="GalleryTheme" parent="GalleryTheme.Base" />

    <style name="GalleryDetailTheme" parent="Theme.AppCompat.NoActionBar" />

    <!-- Base theme applied no matter what API -->
    <style name="GalleryTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--If you are using revision 22.1 please use just windowNoTitle. Without android:-->
        <item name="android:windowNoTitle">true</item>
        <!-- Set theme colors from http://www.google.com/design/spec/style/color.html#color-color-palette-->
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!-- colorAccent is used as the default value for colorControlActivated
             which is used to tint widgets -->
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>

        <!--
        <item name="colorControlNormal">@color/accent</item>

        <item name="colorControlHighlight">@color/accent</item>

        <item name="colorControlActivated">@color/accent</item>

        <item name="colorSwitchThumbNormal">@color/accent</item>

        <item name="colorControlNormal">@color/accent</item>
         -->
    </style>

</resources>
